import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color.fromRGBO(255, 143, 33, 1);
  static const Color primaryLight = Color(0xFFFFB066);
  static const Color primaryDark = Color(0xFFE07010);

  static const Color accentBlue = Colors.blueAccent;
  static const Color accentGreen = Colors.green;
  static const Color accentPurple = Colors.purpleAccent;
  static const Color accentRed = Colors.redAccent;
  static const Color grey = Colors.grey;

  static const Color background = Colors.white;
  static const Color textDark = Color(0xFF222222);
  static const Color textMuted = Color(0xFF757575);
}
